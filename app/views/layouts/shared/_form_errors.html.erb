<% unless f.object.errors.empty? %>
  <div class="alert alert-error">
    <strong>Please try again after fixing the following mistakes:</strong><br/>
    <ul>
      <% f.object.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>